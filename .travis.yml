language: go

go:
  - 1.5

before_install:
  - export $TRAVIS_BUILD_DIR/server
install:
  - cd $GOPATH/src/redbutton
  - go get -t ./...
  - go build ./...

script:
  - cd $GOPATH/src/redbutton
  - go vet -x ./...
  - go test -v ./...
  - go test -covermode=count -coverprofile=profile.cov .